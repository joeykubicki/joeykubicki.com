# macOS
.DS_Store
**/.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp

# Local Claude Code settings (settings.json is shared; .local.json is per-machine)
.claude/settings.local.json

# Node — not used by the root site, but nested projects may pull it in
node_modules/
npm-debug.log*
yarn-error.log*

# Cloudflare / wrangler
.wrangler/
.dev.vars

# Env files — never commit secrets
.env
.env.*
